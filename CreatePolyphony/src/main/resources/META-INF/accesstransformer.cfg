# Create: Polyphony - Access Transformers
#
# Format: <access> <fully.qualified.Class> [member]
#   public           <- widen visibility to public
#   public-f         <- public + remove final
#
# Add entries here as needed when our Mixins or implementation classes need to
# touch private/package-private members of Create or Sound of Steam.
#
# Currently empty: our Mixin into TrackerBarBlockEntity uses @Inject on the
# public handleNote(ShortMessage) method, so no AT is required at this time.
